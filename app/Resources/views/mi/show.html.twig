{% extends 'base.html.twig' %}

{% block body %}
    <h1>Mi</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ mI.id }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ mI.description }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('mi_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('mi_edit', { 'id': mI.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
